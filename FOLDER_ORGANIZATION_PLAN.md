# フォルダ整理計画

## 🎯 目標状態

```
AcademicRepository/                    ← Root: Blockchain + Backend (Next.js)
├── src/                               ← Next.js アプリケーション
│   ├── app/                           ← App Router (ページ)
│   ├── lib/                           ← ユーティリティ
│   └── server/                        ← API ルート等
├── contracts/                         ← Solidity コントラクト
├── scripts/                           ← デプロイ・テストスクリプト
├── test/                              ← Hardhat テスト
├── academic-chain/                    ← DAO UI プロジェクト (Vite)
│   ├── src/                           ← React アプリケーション
│   ├── public/                        ← 静的アセット
│   └── package.json
├── academic-repository/               ← ドキュメント
├── docs/                              ← 仕様書等
├── prisma/                            ← DB スキーマ
├── ignition/                          ← Hardhat Ignition
└── .vscode/, .github/ etc.            ← 設定

```

## ✅ 実施内容

1. ✅ root の `src/` 構造は正しい（Next.js 用）
2. ✅ academic-chain/ の `src/` は独立している（Vite 用）
3. ✅ テンポラリファイルは削除済み

## 📝 現在の状態（実際）

```
✅ Root: src/, contracts/, scripts/, test/ が正しく配置
✅ academic-chain/: src/, public/, package.json が正しく配置
✅ ドキュメント: docs/, academic-repository/ が正しく配置
```

## 🔍 確認が必要な項目

- [ ] すべてのファイルが正しい場所にある
- [ ] 重複がない
- [ ] 不要なファイルがない
